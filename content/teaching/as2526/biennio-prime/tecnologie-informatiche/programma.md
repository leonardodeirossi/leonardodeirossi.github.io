---
title: "Programma didattico"
layout: "corso-programma"

calendario:
  - giorno: "Ogni martedì"
    orario: "11:20 / 13:00"
    aula: "Aula 310"

programma:
  - titolo: "Argomento 1 - I fogli di calcolo"
    argomenti: |
        * Introduzione ai fogli di calcolo:
            * Definizione di "*cella*";
            * Valori ammessi;
            * Formule e operazioni di base.
        * Le funzioni condizionali:
            * Funzioni semplici (<code>=CONTA.SE()</code> e <code>=SOMMA.SE()</code>);
            * Funzioni avanzate (<code>=CONTA.PIÙ.SE()</code> e <code>=SOMMA.PIÙ.SE()</code>).

  - titolo: "Argomento 2 - L'architettura del calcolatore"
    argomenti: |
        * Introduzione ai calcolatori;
        * Cenni storici:
            * Le macchine *Colossus* e *ENIAC*.
        * Definizione e caratteristiche di un programma:
            * Finezza, ordine, non-ambiguità ed eseguibilità.
        * L'architettura di von Neumann;
        * Componenti di un processore:
            * Unità di controllo, ALU, shifter e registri (cenni).

  - titolo: "Argomento 3 - Il sistema operativo Windows"
    argomenti: |
        * Il sistema operativo *MS-DOS*;
        * Versioni di Windows basate su MS-DOS;
        * Versioni di Windows 9.x:
            * Windows 95, 98 e Millennium.

  - titolo: "Argomento 4 - Gli algoritmi e i linguaggi"
    argomenti: |
        * Definizione di *problema* e di *soluzione*;
        * Il concetto di *algoritmo*;
        * Codica degli algoritmi tramite flow-chart:
            * I blocchi che compongono un flow-chart.
        * L'istruzione di selezione:
            * Selezione semplice;
            * Selezione doppia.
        * L'istruzione di iterazione:
            * Iterazione definita;
            * Iterazione indefinita.
        * Rappresentazione di un algoritmo con Flowgorithm (cenni).

  - titolo: "Argomento 5 - Programmazione con Python"
    argomenti: |
        * Introduzione a Python;
        * Configurazione dell'ambiente di programmazione (IDE);
        * Il programma "*Hello, World!*";
        * Sintassi di base:
            * La funzione <code>print()</code>;
            * L'istruzione di assegnazione.
        * Gli errori in Python:
            * Errore di compilazione (*compilation-time*);
            * Errori di esecuzione (*run-time*).
        * I tipi di dato in Python;
        * Gestione dei valori costanti;
        * Operazioni aritmetiche:
            * Operazioni elementari;
            * Divisione tra numeri interi;
            * Calcolo del resto (modulo).
        * Gestione dell'I/O in Python:
            * Funzione <code>print()</code>;
            * Funzione <code>input()</code>.
        * Casting delle variabili;
        * Istruzioni di selezione in Python.
---